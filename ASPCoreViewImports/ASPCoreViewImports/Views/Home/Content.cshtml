@model List<Student>
@{
    ViewData["Title"] = "Content";
}

<h1>Content</h1>

@{
    foreach (var items in Model)
    {
        <p>@items.Id</p>
        <p>@items.Name</p>
        <p>@items.Gender</p>
    }
}