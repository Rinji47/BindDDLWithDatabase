@model List<Student>
@{
    ViewData["Title"] = "About";
}

<h1>About</h1>

@{
    foreach (var items in Model)
    {
        <p>@items.Id</p>
        <p>@items.Name</p>
        <p>@items.Gender</p>
    }
}